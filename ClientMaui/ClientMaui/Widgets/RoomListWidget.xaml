<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ClientMaui.Widgets.RoomListWidget">
    <Border StrokeThickness="1" StrokeShape="RoundRectangle 10,10,10,10" Margin="10">
        <Border.Stroke>
            <LinearGradientBrush EndPoint="0,1">
                <GradientStop Color="Red"
                              Offset="0.1" />
                <GradientStop Color="Green"
                              Offset="1.0" />
            </LinearGradientBrush>
        </Border.Stroke>
        <VerticalStackLayout Margin="10">
            <Label 
                x:Name="RoomHeader"
                Text="Room #"
                VerticalOptions="Center" 
                HorizontalOptions="Center"
                Style="{StaticResource SubHeadline}"
            />
            <Label x:Name="RoomSecurity"  VerticalOptions="Center" HorizontalOptions="Center"></Label>
            <Button x:Name="ConnectButton" x:FieldModifier="public" Text="Připojit" HorizontalOptions="Center" Margin="20,20,20,0"></Button>
        </VerticalStackLayout>

    </Border>
    
</ContentView>
